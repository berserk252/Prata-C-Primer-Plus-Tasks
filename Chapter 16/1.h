#include<stdio.h>
#include<stdlib.h>
#include<time.h>
#include<ctype.h>
#include<string.h>
#include<stdarg.h>
#include<math.h>
#define SIZE 50
#define NUM 5
#define INVERT(x, y) ~(((~x) +(~y)) / 2)
#define A if(a > b) { c = (double)a - (double)b; } else { c = (double)b - (double)a; }
